{
  "name": "ui5.template",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"ui5 serve --port 8080\" \"browser-sync start --proxy 'localhost:8080' --files 'webapp/**/*'\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@ui5/cli": "^4.0.5",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}